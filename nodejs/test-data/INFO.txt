Use `npm run generate:test-fixture` from `/Users/lafifi/Codes/VP/cac-demat/app/nodejs`.
Use `npm run generate:perf-fixture` to generate a larger performance dataset with 40 candidates, 20 members, and 20 reviewers.

The command generates a reusable dataset in `test-data/generated/rich-fixture` with:
- PDFs at the root and in nested folders
- filenames with accents, apostrophes, spaces, and mixed casing
- RTF/DOCX samples to exercise document conversion
- reviewer imports in CSV and XLSX
- member imports covering all-files, root-only `.`, folder references, wildcards, name matching, duplicates, case-insensitive merges, and missing references
- hidden rows in XLSX files to validate spreadsheet filtering

The generated folder can be deleted and recreated at any time.
